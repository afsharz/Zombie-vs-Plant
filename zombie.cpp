#include "zombie.h"

Zombie::Zombie(int h,int fmd,int md,int ap,int tba)
    :health(h),firstMovementDelay(fmd),
    MovementDelay(md),attackPower(ap),
    timeBwAttack(tba)
// first initialze list for each type of zombie all attributs
{

    // setpixmap()
    //setpos->at first
}
//move function
//attack function
